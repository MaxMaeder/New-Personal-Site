<div id="toast"></div>

<style is:global>
  #toast {
    background-color: black;
    color: white;

    font-weight: 500;
    font-size: 1rem;
    padding: 0.5rem 0.75rem;

    position: fixed;
    right: 1rem;
    top: 4.125rem; /* 3.125rem is height of header */
    z-index: 1000;

    opacity: 0;
    transform: translateY(-1.5rem);

    transition: all 0.3s ease;
  }
  #toast.visible {
    opacity: 1;
    transform: none;
  }
</style>